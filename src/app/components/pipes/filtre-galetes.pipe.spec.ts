import { FiltreGaletesPipe } from './filtre-galetes.pipe';

describe('FiltreGaletesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreGaletesPipe();
    expect(pipe).toBeTruthy();
  });
});
